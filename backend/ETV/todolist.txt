handleRegistration Ok

handleCreateTeam :
const apiTeam = await createTeam({
    name: teamName, !!=> teamName : teamName
    encryptedTeamKey: uint8ToBase64(encryptedTeamKeyForCreator),
  });

teamApi: getEncryptedTeamKey apiClient.get(`/teams/${teamId}/key`); => /${teamId}/keys

handleAddMemberToTeam 
service addMemberToTeam hiện gửi body { userId, encryptedTeamKey } (không có memberId trong URL), trong khi backend hiện tại dùng {userId} trong route

